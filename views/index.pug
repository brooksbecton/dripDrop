extends layout

block content
  h1 dripDrop
  form(action="/usersZip/save", method="post")
    input(type="text", name="zip", placeholder="Zip")
    br
    input(type="text", name="uid", placeholder="User's ID")
    br
    input(type="submit")